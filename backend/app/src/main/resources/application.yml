spring:
  datasource:
   
    url: jdbc:postgresql://localhost:5433/livebus
    username: admin
    password: root
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true 
    properties:
      hibernate:
        format_sql: true